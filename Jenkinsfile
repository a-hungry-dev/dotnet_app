pipeline {
    agent { docker 'mcr.microsoft.com/dotnet/core/sdk:2.1' }
    stages {
        stage('ell ess') {
            steps {
                sh (
                    script: 'ls',
                    returnStdout: true
                )
            }
        }
    }
}